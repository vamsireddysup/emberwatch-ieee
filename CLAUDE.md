# Claude Project Context

This repository is shared between the hardware team, Claude, and Codex. Start with:

1. `docs/PROJECT_STATE.md` for the current facts and next work.
2. `docs/ARCHITECTURE.md` for system boundaries.
3. `docs/ML_PIPELINE.md` for data leakage rules and model commands.
4. `docs/HARDWARE_INTEGRATION.md` and `docs/PROTOCOL.md` for the firmware contract.
5. `docs/COLLABORATION.md` before editing.

Keep the central claim precise: EmberWatch detects thermal behavior at a transformer,
recloser, or related grid asset that could become an ignition source. It is not another
forest sensor that detects a fire after ignition.

Do not delete files. Follow the archive process in `archives/INDEX.md`. After material
work, update the project state and decision log and leave exact verification commands.
